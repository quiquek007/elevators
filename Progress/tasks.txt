make elevator animations up/down
highlight the current elevator floor(title and platform)
focus/centrate camera

*get the same properties and fns from common class(extends)
*validation for localStorage data
*make limit for generation of store
*separate components on modules
